{
  "name": "rwa-terminal-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cron Worker: reads Aave Horizon (RPC + issuer APIs) -> Cloudflare KV (live) + Neon (history)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.6.0",
    "@cloudflare/workers-types": "^4.20241127.0"
  }
}
