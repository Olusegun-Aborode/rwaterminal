# Envio codegen outputs (regenerated by `envio codegen` on every build)
.envio/
envio-env.d.ts
generated/

# deps
node_modules/

# local-only test config (RPC data source; never committed)
config.local.yaml
